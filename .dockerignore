# Docker build context exclusions
# This prevents stale local build artifacts from contaminating container builds
# CRITICAL: Docker builds must start from scratch to ensure reproducibility

# All node_modules (will be installed fresh in container)
node_modules/
**/node_modules/

# All dist folders (will be built fresh in container)
dist/
**/dist/

# Turbo cache (prevents cross-platform cache contamination)
.turbo/

# ALL Rust/Cargo build artifacts - must build from scratch in container
# This is critical because macOS-built WASM may differ from Linux-built WASM
target/

# WASM transpilation outputs (will be regenerated in container)
frontend/src/wasm/*/
packages/wasm-*/wasm/
packages/wasm-*/wasm-sync/
packages/mcp-wasm-server/mcp-server-*/
packages/web-agent-core/wasm/
packages/web-agent-core/src/wasm/
packages/wasi-shims/browser-dist/

# Test artifacts
frontend/test-results/
frontend/playwright-report/

# IDE and editor
.vscode/
.idea/

# Git (not needed in build context)
.git/

# Environment and secrets
.env*
!.env.example

# Logs
*.log

# OS files
.DS_Store
Thumbs.db
