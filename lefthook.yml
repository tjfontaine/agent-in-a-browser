pre-push:
  commands:
    rust-fmt:
      run: pnpm exec moon run runtime:fmt-check
      fail_text: "Rust formatting check failed. Run 'cargo fmt --all' to fix."
    rust-check:
      run: pnpm exec moon run runtime:check
      fail_text: "Rust build has warnings or errors. Fix them before pushing."
