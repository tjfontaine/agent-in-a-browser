$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  build:
    script: "tsc && node scripts/build-browser.mjs"
    inputs:
      - "src/**"
      - "scripts/**"
      - "package.json"
      - "tsconfig.json"
    outputs:
      - "dist"
      - "browser-dist"
