$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  build:
    deps:
      - "wasm-loader:build"

  transpile:
    command: "node ../../scripts/transpile.mjs edtui-module"
    inputs:
      - "/target/wasm32-wasip2/release/edtui_module.wasm"
      - "/scripts/transpile.mjs"
    outputs:
      - "wasm"
    deps:
      - "runtime:build-wasm"

  transpile-sync:
    command: "node ../../scripts/transpile.mjs edtui-module --sync"
    inputs:
      - "/target/wasm32-wasip2/release/edtui_module.wasm"
      - "/scripts/transpile.mjs"
    outputs:
      - "wasm-sync"
    deps:
      - "runtime:build-wasm"
