$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  transpile:
    command: "node ../../scripts/transpile.mjs sqlite-module"
    inputs:
      - "../../target/wasm32-wasip2/release/sqlite_module.wasm"
      - "../../scripts/transpile.mjs"
    outputs:
      - "wasm"
    deps:
      - "runtime:build-wasm"

  transpile-sync:
    command: "node ../../scripts/transpile.mjs sqlite-module --sync"
    inputs:
      - "../../target/wasm32-wasip2/release/sqlite_module.wasm"
      - "../../scripts/transpile.mjs"
    outputs:
      - "wasm-sync"
    deps:
      - "runtime:build-wasm"
