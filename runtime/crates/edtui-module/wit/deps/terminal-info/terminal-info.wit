package terminal:info@0.1.0;

interface size {
    /// Terminal dimensions
    record dimensions {
        cols: u16,
        rows: u16,
    }
    
    /// Get current terminal dimensions
    get-terminal-size: func() -> dimensions;
}
