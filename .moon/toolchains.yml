$schema: "https://moonrepo.dev/schemas/toolchain.json"

javascript:
  packageManager: pnpm

node:
  version: "22.22.0"

pnpm:
  version: "9.15.4"

rust:
  components:
    - "rustfmt"
  targets:
    - "wasm32-wasip2"
  bins:
    - "cargo-component"
    - "wit-deps-cli"
    - "wasm-tools"
